package net.example.delegate;

/**
 * A compilation unit whose file name does not match any contained type and
 * that has no public type, so JavaParser reports no primary type. This drives
 * the lookup of a named type that is not the first declared one.
 */
class FirstHelper {
    void a() {
        // first declared type
    }
}

class SecondHelper {
    void b() {
        // second declared type
    }
}
